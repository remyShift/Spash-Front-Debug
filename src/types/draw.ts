export interface DrawingConfig {
    strokeStyle: string;
    lineWidth: number;
    font: string;
}

export interface BoundingBoxDimensions {
    x: number;
    y: number;
    width: number;
    height: number;
}